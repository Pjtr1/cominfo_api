run "pip install -r requirements.txt" in command prompt(win+R > "cmd">enter)
to use the script
for local server

start command for render
gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT