run "pip install -r requirements.txt"
for local server

start command for render
gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT
uvicorn main:app --host 0.0.0.0 --port $PORT (new one removed gunicorn)